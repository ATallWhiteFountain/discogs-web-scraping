This is a Discogs API web scraping script.
For a selected band (using band's ID), it searches the Discogs' resources,
and prints other bands (and members) if they have same members as band 
asked for. In addition to printing these bands, also the members that 
reappear are printed.
